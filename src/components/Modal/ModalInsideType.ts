export type ModalInsideInfoType = {
  addTitle: string;
  setTitle: (text: string) => void;
  addInfo: string;
  setInfo: (text: string) => void;
  placeholderTitle: string;
  placeholderInfo: string;
};
